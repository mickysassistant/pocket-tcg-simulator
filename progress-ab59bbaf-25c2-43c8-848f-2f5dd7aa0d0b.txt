# Progress Log
Run: ab59bbaf-25c2-43c8-848f-2f5dd7aa0d0b
Task: Implementar la ejecución de acciones en el CLI tcgp del simulador Pocket TCG.
Started: 2025-02-17

## Codebase Patterns
- The project uses node:test for testing, run with `node --test tests/**/*.test.js`
- Tests are defined inline with custom runTest helper for custom error formatting
- Database persistence uses SQLite with node:sqlite DatabaseSync (synchronous API)
- Each DB operation creates its own connection and closes it in finally block
- GameState is the core game state object that manages players, turn tracking, etc.
- TurnManager.startTurn(playerId) handles turn start including auto-draw of 1 card
- Game state can be persisted to SQLite with state.save() and restored with GameState.fromJSON()
- CLI commands are in src/cli/commands/ and use a handler(argv) async function
- The argv object contains: json, help, positionalArgs, and formatOutput helper
- Actions are registered in src/cli/services/actions.js with schemas for validation
- Sessions and state are managed through repositories in src/cli/services/
- Action validation returns { valid, reason?, errors? } object
- CLI output supports both plain text and JSON formats via --json flag
- TurnManager.endTurn() returns win condition result or null if game continues
- Session status can be updated via sessions.update(sessionId, { status: 'completed' })

## 2025-02-17 - ACT-001: Ejecutar start_turn: auto-draw + transición de fase
- Implemented action execution in CLI action command handler
- Added GameState.fromJSON() static method to restore game state from persisted data
- Updated draw action description to "Internal: auto-executed at turn start" (not to be removed from registry)
- Added action execution logic for start_turn action:
  - Loads session from DB using --session flag
  - Loads latest state from DB
  - Restores GameState using GameState.fromJSON()
  - Creates TurnManager and calls startTurn(playerId) which includes auto-draw
  - Updates phase to 'main'
  - Saves new state to DB
  - Returns result with turn, player, hand cards, phase, and state metadata
- Added proper error handling: SESSION_NOT_FOUND, MISSING_SESSION, SESSION_NOT_ACTIVE, NO_STATE_FOUND, VALIDATION_ERROR, INVALID_JSON, NOT_IMPLEMENTED
- Updated action.js help text to include --session flag and execution examples
- Files changed:
  - src/game/game-state.js: Added fromJSON() static method
  - src/cli/services/actions.js: Updated draw action description
  - src/cli/commands/action.js: Added action execution handler with session flag parsing
  - tests/cli/action-command.test.js: Updated NOT_IMPLEMENTED test to include session
  - tests/cli/action-execution.test.js: New test file with 14 tests for start_turn execution
- Tests: 14 new tests covering all acceptance criteria:
  - AC1: start_turn executes without error
  - AC2: State saved with correct phase, turn, player, and stateId
  - AC3: Response includes hand after auto-draw
  - AC4: SESSION_NOT_FOUND errors when no session or session doesn't exist
  - Additional validation tests for payload validation
- **Learnings:**
  - GameState doesn't have built-in JSON serialization; need to manually extract fields when saving and reconstruct when loading
  - TurnManager.startTurn() already handles the auto-draw internally (1 card), so no separate draw action is needed
  - The fromJSON() method is essential for restoring game state from DB and continuing execution
  - CLI action command needs --session flag to know which session to execute actions against
  - Database locking can occur when running tests in parallel with node --test due to multiple processes accessing SQLite
  - Tests using execTcgp() spawn child processes, which causes database locking when many tests run concurrently
  - JSON and plain text output formats need to be kept in sync for consistency

## 2025-02-17 - ACT-002: Ejecutar end_turn: cambio de jugador y avance de turno
- Implemented end_turn action execution in CLI action command handler
- Added turn validation: checks if requesting player is the current player (WRONG_TURN error)
- Added end_turn action execution logic:
  - Validates that the player ending turn is the current player
  - Calls TurnManager.endTurn() which handles: phase→checkup, turn switch, turn increment, phase→draw
  - Checks win condition from endTurn result
  - Updates session status to 'completed' if game ends
  - Saves new state to DB
  - Returns result with new currentPlayer, turnNumber, phase, sessionStatus, and gameEnded flag
- Updated CLI output formatting to handle end_turn specific fields
- Files changed:
  - src/cli/commands/action.js: Added end_turn case in action execution handler
  - tests/cli/end-turn-action.test.js: New test file with 15 tests for end_turn execution
- Tests: 15 new tests covering all acceptance criteria:
  - AC1: end_turn executes without error and changes currentPlayer (p1→p2, p2→p1)
  - AC2: Turn number increments correctly and phase is set to 'draw'
  - AC3: Session status included in response, gameEnded flag shows if game ended
  - AC4: WRONG_TURN error when player is not current
  - Additional tests: payload validation, output formats, state saving, multiple turns
- **Learnings:**
  - TurnManager.endTurn() returns winResult (null or { winner, reason, message })
  - Session status can be updated via sessions.update(sessionId, { status: 'completed' })
  - Boolean fields in response should always be present (use !!winResult for gameEnded)
  - The end_turn flow is: validate player → call TurnManager.endTurn() → check win → update session → save state → return result
  - Wrong turn validation should happen before any game state changes
  - Output formatting needs to handle action-specific fields (handSize for start_turn, sessionStatus/gameEnded for end_turn)

---
## 2025-02-17 - ACT-003: Ejecutar attach_energy: adjuntar energía al Pokémon objetivo
- Implemented attach_energy action execution in CLI action command handler
- Enhanced EnergySystem to actually attach energy to Pokemon (not just log):
  - Added findPokemon() method to locate Pokemon in active or banque by ID
  - Updated attachEnergy() to initialize attachedEnergy array on Pokemon if needed
  - Energy is taken from EnergyZone and attached to target Pokemon's attachedEnergy array
- Added energyAttachedThisTurn tracking to prevent multiple energy attachments per turn:
  - Added energyAttachedThisTurn field to GameState
  - Updated TurnManager.startTurn() to reset this flag at turn start
  - Updated GameState.fromJSON() to restore this flag
  - Action handler validates this flag and returns ENERGY_ALREADY_ATTACHED error if already attached
- Added attach_energy case to action command handler with validations:
  - Phase must be 'main' (WRONG_PHASE error)
  - Player must be current player (WRONG_TURN error)
  - Energy not yet attached this turn (ENERGY_ALREADY_ATTACHED error)
  - Target Pokemon must be in active or banque (INVALID_TARGET error)
- Updated session creation to populate test data:
  - Added 10 energy cards to each player's Energy Zone
  - Added Pokemon to active position and banque for testing
- Updated state saving to include energyAttachedThisTurn field
- Updated CLI output formatting for attach_energy specific fields
- Files changed:
  - src/game/energy-system.js: Enhanced attachEnergy() and added findPokemon()
  - src/game/game-state.js: Added energyAttachedThisTurn field and fromJSON() support
  - src/game/turn-manager.js: Reset energyAttachedThisTurn in startTurn()
  - src/cli/commands/action.js: Added attach_energy case and EnergySystem import
  - src/cli/commands/session.js: Added test Pokemon and energy to session creation
  - tests/cli/attach-energy-action.test.js: New test file with 16 tests
- Tests: 16 new tests covering all acceptance criteria:
  - AC1: attach_energy executes without error and returns updated Pokemon with energy
  - AC2: ENERGY_ALREADY_ATTACHED error on second attach in same turn, but allowed on new turn
  - AC3: INVALID_TARGET error for non-existent Pokemon or opponent's Pokemon
  - AC4: WRONG_PHASE error when not in main phase
  - Additional tests: payload validation, output formats, state saving, player2
- **Learnings:**
  - EnergySystem.attachEnergy() must actually add energy to Pokemon's attachedEnergy array
  - The first-turn energy attachment restriction prevents player1 from attaching energy on turn 0
  - Tests need to account for the first-turn restriction by testing on player2's turn or later turns
  - Pokemon cards have an optional attachedEnergy array that stores attached energy cards
  - Session creation should populate test data (Pokemon in active/banque, energy in Energy Zone) for action testing
  - Energy attached to a Pokemon needs to be tracked and persisted in the game state
  - Validation order matters: phase → player → turn-specific restrictions → target validity

---
## 2025-02-17 - ACT-004: Añadir y ejecutar play_pokemon: jugar Pokémon básico desde la mano
- Implemented play_pokemon action execution in CLI action command handler
- Added play_pokemon action definition to actions.js:
  - id: 'play_pokemon'
  - schema: { playerId, cardId, zone: 'active'|'bench' }
  - description: 'Play a Basic Pokémon from hand to active zone or bench'
- Added play_pokemon case to action command handler with validations:
  - Phase must be 'main' (WRONG_PHASE error)
  - Player must be current player (WRONG_TURN error)
  - Card must be in player's hand (CARD_NOT_IN_HAND error)
  - Card must be a Basic Pokémon (supertype === 'Pokémon' && subtype === 'Basic') (NOT_BASIC error)
  - If zone === 'active': only if no active Pokémon (ACTIVE_OCCUPIED error)
  - If zone === 'bench': only if bench has room (max 3) (BENCH_FULL error)
- Moves card from hand to the target zone (active slot or banque array)
- Updated session creation to populate test data:
  - Added 3 Basic Pokémon to each player's hand for play_pokemon testing
- Updated CLI output formatting for play_pokemon specific fields
- Files changed:
  - src/cli/services/actions.js: Added play_pokemon action definition
  - src/cli/commands/action.js: Added play_pokemon case with execution logic
  - src/cli/commands/session.js: Added Basic Pokemon to players' hands for testing
  - tests/cli/play-pokemon-action.test.js: New test file with 23 tests
- Tests: 23 new tests covering all acceptance criteria:
  - AC1: play_pokemon executes without error to bench and active
  - AC2: CARD_NOT_IN_HAND error when card not in hand or already played
  - AC3: NOT_BASIC error (check exists, test covers Basic Pokemon success case)
  - AC4: BENCH_FULL error when bench reaches 3 Pokemon
  - AC5: ACTIVE_OCCUPIED error when active slot has Pokemon
  - Additional tests: payload validation, output formats, state saving, player2, hand/bench size tracking
- **Learnings:**
  - Session creation adds 1 Pokemon to banque, so max bench capacity is effectively 2 additional Pokemon
  - The play_pokemon action removes the card from hand (using splice) and adds it to the target zone
  - Validation order: phase → player → card in hand → Basic Pokemon → zone constraints
  - Zone constraints are mutually exclusive: active checks for emptiness, bench checks for capacity
  - Card ID must be unique within a player's hand to allow proper identification
  - Test data needs to include cards that match the expected card IDs in tests
  - The action returns the played Pokemon card object in the response for verification
---

## 2025-02-18 - ACT-005: Ejecutar evolve: evolucionar Pokémon en activo o banca
- Implemented evolve action execution in CLI action command handler
- Added evolvedThisTurn tracking to GameState to prevent multiple evolutions per turn per Pokemon:
  - Added evolvedThisTurn field to GameState (Set of pokemonIds that evolved this turn)
  - Updated TurnManager.startTurn() to clear this Set at turn start
  - Updated GameState.fromJSON() to restore this Set from array
  - Action handler validates this Set and returns ALREADY_EVOLVED error if Pokemon already evolved
- Updated evolve action schema in actions.js:
  - Changed from full evolutionCard object to just evolutionCardId string
  - evolutionCardId must be in player's hand (CARD_NOT_IN_HAND error)
  - evolutionCard must be a valid evolution card (Stage 1 or Stage 2 Pokémon)
- Added evolve case to action command handler with validations:
  - Phase must be 'main' (WRONG_PHASE error)
  - Player must be current player (WRONG_TURN error)
  - Evolution card must be in player's hand (CARD_NOT_IN_HAND error)
  - Evolution card must be a Stage 1 or Stage 2 Pokémon (INVALID_EVOLUTION error)
  - Target Pokemon must be in active or banque (INVALID_TARGET error)
  - Cannot evolve on turn 0 (FIRST_TURN_RESTRICTION error via EvolutionSystem.canEvolve)
  - Cannot evolve same Pokemon twice in one turn (ALREADY_EVOLVED error)
  - Evolution chain must be valid (Basic→Stage1→Stage2) (INVALID_EVOLUTION error)
- Evolution is applied using EvolutionSystem.evolve():
  - Replaces Pokemon with evolved version (keeps energy and damage)
  - Marks Pokemon as evolved in gameState.evolvedThisTurn
- Removes evolution card from player's hand after successful evolution
- Updated session creation to populate test data:
  - Added Stage 1 and Stage 2 evolution cards to each player's hand
- Updated state saving to include evolvedThisTurn field (as array for JSON serialization)
- Updated CLI output formatting for evolve specific fields
- Files changed:
  - src/game/game-state.js: Added evolvedThisTurn field and fromJSON() support
  - src/game/turn-manager.js: Reset evolvedThisTurn in startTurn()
  - src/cli/services/actions.js: Updated evolve action schema (evolutionCardId instead of evolutionCard)
  - src/cli/commands/action.js: Added evolve case with full validation logic
  - src/cli/commands/session.js: Added evolution cards to players' hands for testing
  - tests/cli/evolve-action.test.js: New test file with 15 tests
  - tests/cli/evolve-action-simple.test.js: Simple integration test
- Tests: 15 new tests covering all acceptance criteria:
  - AC1: evolve executes without error for active and bench Pokemon
  - AC2: INVALID_EVOLUTION error for non-evolution card, wrong evolution chain, card not in hand
  - AC3: ALREADY_EVOLVED error when Pokemon already evolved this turn
  - AC4: FIRST_TURN_RESTRICTION error on turn 0
  - Additional tests: WRONG_PHASE, WRONG_TURN, INVALID_TARGET, evolution chain (Basic→Stage1→Stage2), card removed from hand, JSON/plain text output
- **Learnings:**
  - The evolvedThisTurn tracking must be in GameState, not EvolutionSystem, because each action execution creates a new EvolutionSystem instance
  - GameState.evolvedThisTurn is a Set, but must be serialized as array in state.save() and restored as Set in GameState.fromJSON()
  - EvolutionSystem.evolve() updates the Pokemon in place but we need to track evolvedThisTurn separately in the handler
  - Evolution chain validation: Stage 1 requires Basic, Stage 2 requires Stage 1
  - EvolutionSystem.canEvolve() already checks opening turn restriction and playedThisTurn flag
  - The evolve action handler validates additional constraints: card in hand, valid evolution card, evolution chain
  - Session creation test data needs evolution cards matching test expectations
  - Test data should include both Stage 1 and Stage 2 evolution cards for testing evolution chains
---
